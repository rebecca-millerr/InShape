

git branch <branch-name> 
	creates a branch with an exact copy of master


git checkout <branch-name>
	goes ot that branch



git add -A
	adds everything that you have changed to the commit stage


git commit -m <message>
	puts whatever you staged using git add and gets it ready to be pushed


git push origin <branch-name>
	pushes everything that you have in branch locally to that branch in remote
	(github)


branch --> copied everything from master
local rebeca-miller --> master copy
remote rebeca-miller --> empty

git push:
remote rebeca-miller --> master copy

---------
git pull origin <branch-name>

git clone https://nfklbx;kv 




git remote add origin <url-name>
 --> url
can change origin with another name


