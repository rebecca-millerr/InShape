// module.exports allows this to be used as a module elsewhere
// this is the sequelize equivalent of CREATE TABLE users etc
// this is an example of what this should look like

/*
module.exports = (sequelize, DataTypes) => {
  let User = sequelize.define('User', {
    username: {
      type: DataTypes.STRING,
      primaryKey: true
    },
    email: DataTypes.STRING,
    firstName: DataTypes.STRING,
    lastName: DataTypes.STRING,
    password: DataTypes.STRING,
  });

  return User;
};
*/

// TODO: create table for our project here

